__pycache__/
*.pyc
.venv*/
# ⚠️ `.env` seul laissait passer `.env.preprod`, `.env.sonnet` et consorts — des
# variantes qui apparaissent naturellement dès qu'une box sert deux environnements.
# Le dépôt est PUBLIC : le motif doit couvrir la famille, pas un nom exact.
.env
.env.*
!.env.example
*.egg-info/
# ⚠️ Les journaux par travail (`oto_runner/journal.py`) portent de la DONNÉE de la
# file de travail — lignes clientes, sorties d'outils. Le dépôt est PUBLIC.
passages/
